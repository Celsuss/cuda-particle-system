{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug c++ project, gdb",
            "type": "gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/cuda-particle-system",
            "args": ["-q"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false
        }
    ]
}
